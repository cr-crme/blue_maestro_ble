name: blue_maestro
description: Test of the thermal BLE device

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_reactive_ble: ^5.0.2
  intl: ^0.17.0
  functional_data: ^1.0.0
  meta: ^1.8.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
